<div class="review_form">
<%= form_for [restaurant, @review] do |f| %>
	<%= f.label :thoughts %>
	<%= f.text_area :thoughts %><br>
	<%= f.label :rating %>
	<%= f.select :rating, (1..5) %>
	<%= f.submit %>
<%end%>
</div>